export class AnnouncementCost {
  constructor(
    private helper_number: number,
    private working_hours: number,
    private total_cost: number,
    private workers_cost: number,
    private service_fees: number
  ) {
  }
}
